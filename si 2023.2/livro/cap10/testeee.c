#include <stdio.h>

int unico(int tam, int lista[tam], int elemento)
{
    
}

int main()
{
    int array[100] = {1};

    scanf("%d%d", &array[2], &array[10]);

    for (int i = 0; i < 100; i++) {
        printf("%d", array[i]);
    }

    return 0;
}